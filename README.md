# homebrew-tap

Homebrew formulae for ppiankov tools.

## Install

```bash
# logtap — ephemeral log mirror for Kubernetes load testing
brew install ppiankov/tap/logtap

# mongospectre — MongoDB schema auditor
brew install ppiankov/tap/mongospectre
```
